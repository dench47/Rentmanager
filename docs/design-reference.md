# Эталон: иконки и отступы (канвас «14», звёзды Вики от 19.09)

Единый источник правды для всех экранов с кастомными иконками.
Источник замеров: файл HDlxpKy1uoMuPew3ejDkwW, узлы 3108-56847/56859,
2533-17817, 3463-71139/71157/71175 (+ копии 2992/2995 для сверки).

## CTA-кнопки (55, радиус 100)

| Правило | Значение |
|---|---|
| Иконка в CTA | 24×24 |
| Зазор иконка → текст | **6** (было 10 — правка Вики) |
| Зазор между кнопками в паре | 6 |
| Зазор контент → первая кнопка | 20 |

Варианты (проверять заливку в макете, не угадывать):
- **Чёрная** #212121, текст белый, иконка — белый залитый круг с тёмным плюсом
  (`ic_cta_plus_white`, 27 путей с тенями 0.2);
- **Градиент** 136° F6D85E→E89B5A→D97D5D, текст #212121, иконка графитовая
  (`ic_plus_circle_graphite`) или без иконки;
- **Контурная** белая, рамка 1 #212121/#727272, текст #212121.

Компоненты: `BlackCtaButton` / `GradientCtaButton` / `OutlineCtaButton`
(CreationFlowComponents.kt) — все с `iconSpacing = 6.dp` по умолчанию.

## Шапка экрана (AppScreenHeader)

Статус-инсет (statusBarsPadding) → **27** → стрелка 24 (`ic_landlord_back`,
вектор!) + 8 + текст Inter 20/600/−0.3 → низ **13**, поля 20.
Иконки справа (поиск/уведомления/меню): 24×24, зазор между 8, от края 20.
НЕ использовать `ic_arrow_left.png` (72px, мылится и смещает строку).

## История операций — пустые состояния (3463:71139/71157/71175)

- Блок на белом экране, БЕЗ карточки-контейнера;
- иллюстрация `empty-payment-history` **140×140** (PNG 700 в drawable-nodpi,
  `ic_empty_operations_history`, FilterQuality.High);
- заголовок 20/600/−0.3; подпись 15/600/−0.4 в 2 строки (перенос как у Вики);
- пара CTA на ВСЕХ табах: градиент «Добавить объект» (плюс, зазор 6) +
  контур «Пополнить баланс»;
- заголовки: «Операций/Зачислений/Списаний пока нет».

## Главный экран арендодателя без объектов (2533:17817)

- «Добавьте первый объект» 20/600 + подпись 15/600 (2 строки);
- CTA: чёрная «Добавить первый объект» (плюс в белом круге) → 6 →
  градиент «Управление подпиской» (без иконки).

## Пустые списки арендаторов/арендодателей (3108:56847/56859)

- Иллюстрация empty-contacts 135×135 (PNG 675, nodpi, FilterQuality.High);
- зазоры: иллюстрация→заголовок 20, заголовок(24h)→подпись 12, подпись→CTA 20;
- CTA: чёрная с иконкой «человек+плюс» (`ic_person_plus`, зазор 6);
- блок центрирован по ВСЕЙ высоте кадра (не между шапкой и таббаром).

## Общие правила

- Иллюстрации — PNG 5x в `drawable-nodpi` + `FilterQuality.High`;
  мелкие линейные иконки — VectorDrawable (конвертер tools/svg2vd.py, альфы
  обязательны);
- Экспорт иконок из Figma — только по ID инстанса `I<frame>%3B<component>`
  (по компоненту отдаёт мусор: «корона»/«колокол» вместо нужного);
- lineHeight: 15sp→18.2, 13sp→15.7 явно;
- Обновление этого файла обязательно при новых звёздах Вики.

## Шевроны (зафиксировано Денисом 2026-09-21)

**ВСЕ шевроны в приложении — наш кастомный `ic_card_chevron`** (40×40, глиф
11×5.5, stroke #212121 1.5, вниз-пик). Никаких импортных ic_chevron_right:
- аккордеоны — как есть (вниз), при раскрытии поворот 180°;
- строки-ссылки (вправо) — `graphicsLayer { rotationZ = -90f }`;
- зона тапа 40dp, иконка не меньше рамки 40.

## Каноны экранов (зафиксировано Денисом 2026-09-22)

- **Заголовок экрана всегда нажимаем: тап по названию = тап по стрелке «назад»**
  (вся строка стрелка+название — один кликабельный блок).
- **Тапбар с CTA (низ экрана редактирования)**: панель #EDEDED@90%,
  **верхние углы скруглены 30**, поля 20/10; кнопки СТАТИЧНЫ — клавиатура
  их НЕ поднимает (никакого imePadding на тапбаре), контент скроллится под ней.
- **Баяны по умолчанию ЗАКРЫТЫ** — раскрывает только юзер; шеврон всегда наш
  ic_card_chevron (180° при раскрытии).
- **Паспортные поля в редактировании** — три состояния: пустое (подпись 15/600
  серым в 2 строки, глаз-слэш) / видимое (подпись 13/400 + значение, глаз) /
  скрытое (маска «·· 08»/«······2545», глаз-слэш); глаз переключает видимость.

- **Клавиатура не должна прятать поле ввода** (канон 2026-09-22): `imePadding`
  вешается ТОЛЬКО на скроллящийся контент (вьюпорт сжимается до клавиатуры,
  Compose сам докручивает к полю в фокусе); тапбар с CTA остаётся неподвижным.
- **Числовые поля** (паспорт и т.п.) открывают числовую клавиатуру
  (`KeyboardType.Number`).
- **Пункты меню/шитов всегда рабочие** — «не для красоты написаны»: Сфотографировать
  → TakePicture (FileProvider), Выбрать из галереи → GetContent(image/*),
  Выбрать файл → OpenDocument.
